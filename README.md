After downloading repo do next:

- python -m venv .venv
- .venv\Scripts\Activate.ps1
- python -m pip install -r requirements.txt

To sync ipynb and python files:

- jupytext --sync your_file.ipynb/py
